<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>asdasd</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body>
    <div id="content-wrapper">
   <div id="banner"><h1>Todo-list</h1></div> 

<div id="content-box">
    <div id="inpt-and-lis">
        <% if (todoEdit =="") { %>
            <%- include("add.ejs") %>      
       <% } %>
       <a href="/?page=<%=(dataToShow/dataPerPage)%>&&sorted=1"> Sortera A-Z  </a>
    <% if (todoEdit != ""){ %>
    
        <%- include("edit.ejs") %>
  <%  } %>
  <% if (error != "") { %>
   <div id="add-input-error-div"> <p id="add-input-error-p"><%=error%></p></div>
 <% } %>
 


 <% for(let i = 0; i < data.length; i++){
        %> 
        <div class="todo-box">
        <% let toBeCapped = data[i].name %>
        <% let cappedString = (toBeCapped[0].toUpperCase() + toBeCapped.slice(1)); %>
        <span class="todo-item"> <%=cappedString%> </span> 
        <div class="link-box">
        <a href="/delete/<%= data[i]._id %>" > delete</a>
        <a href="/edit/<%= data[i]._id %>" > edit</a>
    </div>
        </div>
    
     <% } %>
        <% if(totaldata !== dataToShow/dataPerPage){ %>
            <a href="/?page=<%=(dataToShow/dataPerPage)+1%>">Visa fler</a>


       <% }  else { %>
        <a href="/?page=1">Visa mindre</a>
   <% } %>
    </div>
    </div>
    
</div>
</body>
</html>